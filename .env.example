# Environment variable template.
# Duplicate this file to .env (gitignored) and fill values.

# Core application
NEXT_PUBLIC_APP_NAME=NeoChyrp
NEXT_PUBLIC_APP_URL=http://localhost:3000

## Supabase (auth, storage, realtime)
# Public keys (safe in browser; RLS must be enabled!)
NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_PROJECT_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_PUBLIC_KEY
# Service-role (NEVER expose to client code)
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY


## Database (Prisma best practice: pooler for DATABASE_URL, direct for migrations)
# Use connection pooling for Prisma client (runtime). Remove quotes if not needed.
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@YOUR_POOL_HOST:6543/postgres?pgbouncer=true
# Use direct connection for migrations (Prisma will use this automatically for migrate commands)
DIRECT_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_DB_HOST:5432/postgres

## Security / misc (replace in production)
ENCRYPTION_KEY=32_byte_key_for_future_encryption_pad_it
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-with-at-least-32-characters

## Upload limits (bytes)
# Server enforces MAX_UPLOAD_BYTES; the client can also read NEXT_PUBLIC_MAX_UPLOAD_BYTES for preflight validation.
MAX_UPLOAD_BYTES=2097152
NEXT_PUBLIC_MAX_UPLOAD_BYTES=2097152

## Storage
NEXT_PUBLIC_STORAGE_BUCKET=uploads
